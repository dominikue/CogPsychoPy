name: psychopy_cogsci
channels:
  - conda-forge
dependencies:
  - python=3.8
  - doctest
  - yaml
  - pytables # broken for psychopy pip install
  - pip
  - pip:
      - psychopy # to install newest version. conda is outdated
